apiVersion: v1
kind: Service
metadata:
  name: mysql1
  labels:
    name: mysql
    app: java
spec:
  ports:
  - port: 3306
    name: mysql
    targetPort: 3306
  selector:
    name: mysql
    app: java

